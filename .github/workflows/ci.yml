name: CI

on:
  push:
    branches: [main, prod]
  pull_request:
    branches: [ "**" ]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
  CARGO_REGISTRIES_CRATES_IO_PROTOCOL: sparse

jobs:
  # ── Backend (Rust) ──────────────────────────────────────────────

  backend-lint:
    name: Backend lint & format
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt

      - name: Cache Rust dependencies
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: backend

      - name: Check formatting
        run: cargo fmt --all -- --check

      - name: Clippy (lib + bins, no test binaries)
        run: cargo clippy --lib --bins -- -D warnings

      - name: Clippy (test code only)
        run: cargo clippy --tests -- -D warnings

  backend-test:
    name: Backend tests
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt

      - name: Cache Rust dependencies
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: backend

      - name: Run lib tests (fast — no test binaries)
        run: cargo test --lib

      - name: Run doc tests
        run: cargo test --doc

      - name: Run integration tests (no test binaries)
        run: cargo test --tests

  # ── Frontend (TypeScript) ───────────────────────────────────────

  frontend:
    name: Frontend tests & build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Type check
        run: npx tsc --noEmit

      - name: Lint
        run: npm run lint

      - name: Run unit tests
        run: npm test -- --run

      - name: Build
        run: npm run build

  # ── Security scans (Trivy) ──────────────────────────────────────

  trivy-repo:
    name: Trivy repo scan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run Trivy vulnerability scanner in repo mode
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          scan-type: fs
          scan-ref: .
          exit-code: 1
          severity: CRITICAL
          format: table

  trivy-config:
    name: Trivy config scan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run Trivy config scanner
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          scan-type: config
          scan-ref: .
          exit-code: 1
          severity: CRITICAL
          format: table

  # trivy-images:
  #   name: Trivy Docker image scan
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - name: Build backend image
  #       run: docker build -t backend:scan backend/
  #     - name: Build frontend image
  #       run: docker build -t frontend:scan frontend/
  #     - name: Scan backend image
  #       uses: aquasecurity/trivy-action@v0.36.0
  #       with:
  #         image-ref: backend:scan
  #         exit-code: 1
  #         severity: CRITICAL
  #         format: table
  #     - name: Scan frontend image
  #       uses: aquasecurity/trivy-action@v0.36.0
  #       with:
  #         image-ref: frontend:scan
  #         exit-code: 1
  #         severity: CRITICAL
  #         format: table
